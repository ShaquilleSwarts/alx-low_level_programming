#include "main.h"
/**
 * _memset - fill memory
 * Description: fills memory with a constant byte
 * @*s: param is *s
 * @b : param is b
 * @n : param is n
 * Return: Returns 0
 */
char *_memset(char *s, char b, unsigned int n)
{
  _memset (s, b, n);
  return (s);
}
